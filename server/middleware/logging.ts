// Middleware do servidor
// Pode ser usado para logging, autenticação, etc.

export default defineEventHandler((event) => {
  // Middleware exemplo - adicionar headers, logging, etc.
  // console.log('Request:', event.node.req.url)
})
